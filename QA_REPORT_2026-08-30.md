# 乙抓记录｜第二轮全量 QA 与稳定性专项报告

日期：2026-08-30  
Git checkpoint：`e9dfb13`（`checkpoint: before second full QA`）

## 范围与执行边界

本轮没有新增功能，没有修改 HTML/CSS，没有改变模板视觉、模板尺寸、主题色、产品数据结构或正常业务流程。

依据仓库工作约定，本轮未运行浏览器自动化、截图对比、真实设备操作或端到端点击测试。完成的是全量源码追踪、静态检查、纯逻辑自动测试、异常数据推演和资源生命周期审查。需要真实浏览器、iOS Safari 或视觉确认的项目列入“保留风险 / 人工验证清单”。

## 第一阶段：功能地图与数据链路

### 页面与入口

- 模板编辑：模板、主题、字体、数据导入/导出、PNG 导出、清空、存储管理、收藏导入/保存。
- 独立修图：上传、马赛克、模糊、白雾、橡皮擦、贴纸、撤销/重做、移除、导出。
- 我的收藏：网格/列表、分页、筛选、排序、标签管理、批量操作、详情编辑、RJ 导入、备份导入/导出、导回模板。

### 模板

- `full`：1080 × 1440，首页 + 多个文字续页。
- `compact`：600 × 800 预览，Canvas 以 1.8 倍导出为 1080 × 1440，首页 + 多个文字续页。
- `grid9`：九宫格，1080 × 1440。
- `quick`：12 格速评，1080 × 1440。
- `trio`：三宫格，1080 × 1440。

### 字段与交互

- 标题、CV、社团/作者、RJ、时长、购买/收听日期、价格、折扣、中文版本、评分、关键词、评论。
- 完整版首页长评使用移动端专注编辑层；完整版续页为 textarea；简略版续页为 `contenteditable`。
- 格子模板分别保存每格 BK、完整/裁切模式、RJ、CV/短标题/短评/价格/评分等模板专属字段。
- 中文/日文输入法 composition、粘贴纯文本、字段长度限制和自动保存均已追踪。

### 图片链路

文件或 RJ 封面 → data URL → 尺寸/格式压缩 → DOM 预览或图片编辑器 → `collectState()` → 图片 Blob 引用序列化 → 图片 IndexedDB。

恢复时执行相反流程；引用存在但 Blob 缺失时尝试按远程元数据重建，无法重建则清空该图片并显示站内错误，不中断整页状态恢复。

### 状态与持久化

- 应用状态 IndexedDB：`otome-record-card-state-v1`，stores 为 `state`、`settings`、`editor-projects`。
- 图片 IndexedDB：`otome-record-card-images-v1`，stores 为 `images`、`image-meta`。
- 收藏 IndexedDB：`otome-record-card-collection`，store 为 `works`。
- `localStorage`：轻量 UI 偏好、标签偏好和旧版本迁移入口。
- `collectState()` / `applyState()` 是 DOM 与持久化快照的统一边界。
- 保存采用 80 ms 防抖和串行 Promise 队列；模板切换、切页、新增/删除页、图片应用和导入等关键操作会直接触发保存。

### 导入导出

- 项目 JSON：公开格式保持 `{ app, version:1, exportedAt, data }`，兼容旧裸 state。
- 收藏 JSON：版本 1/2/3 兼容。
- PNG：DOM/CSS 负责预览，Canvas 负责导出；五个模板均固定输出 1080 × 1440。
- 多页：按首页到最后续页顺序逐页渲染；PC 逐页下载，移动端使用可切页预览弹层。

## 实际发现的 Bug

### 中等

1. 项目 JSON 或旧 `localStorage` 状态若是数组，会被误判为合法对象。导入 `[]`、`{ "data": [] }` 或旧状态 `[]` 时，可能把当前字段应用为空值并继续持久化。
2. 旧简略版续页快照若是语法合法但类型错误的数组，会被规范化成空续页，并覆盖 IndexedDB 中本来正常的简略版续页状态。
3. 全局 `contenteditable` 清理只识别主模板字段的 composition 状态。标签、收藏详情、播放器时间、简略版续页等富文本字段在中文/日文输入法组合期间，如果浏览器插入临时元素，可能被提前扁平化并移动光标。

### 较低

4. 收藏标签 `localStorage` 值若是 `{}`、数字或混合类型数组等“合法 JSON、错误类型”，收藏标签计算可能抛错，导致收藏模块初始化失败。
5. 收藏 IndexedDB 若混入 `null`、非对象或缺少 ID 的坏记录，后续渲染可能因字段访问或 keyPath 写回失败而中断收藏加载。

## 已修复

- 新增统一普通记录对象判断：拒绝数组、`null` 和其他非记录类型。
- 项目 JSON wrapper 的 `data` 必须是普通对象；旧裸 state 仍保持兼容。
- IndexedDB 三段状态合并只接受普通对象；缺失或损坏的 settings/editor 分片不会污染 draft。
- 旧 `localStorage` 状态和旧简略版续页快照只在结构有效时参与迁移。
- 收藏标签偏好统一按字符串数组容错解析；非法 JSON、错误容器类型和非字符串成员安全降级。
- 收藏加载时仅在内存中采用结构有效且有 ID 的记录；坏记录不被自动删除或覆盖。
- 所有 `contenteditable` 统一加入 composition start/end 保护；组合结束后再触发既有规范化与保存路径。

## 自动测试

新增无依赖的 `qa-tests.js`，直接从生产脚本提取目标函数执行，不引入测试框架。

覆盖：

- 普通对象识别与异常 JSON 类型。
- 收藏字符串数组偏好解析。
- 项目 JSON wrapper / 旧裸 state 解析。
- 续页规范化、当前页边界和坏快照拒绝。
- 日期、折扣和现价格式化。
- RJ 批量解析、重复值和非法值。
- 收藏分页边界。
- 状态拆分/合并和损坏分片。
- 图片引用的递归映射与引用收集。
- 全局 composition 防护回归断言。

## 已验证

实际执行并通过：

- `node --check js/app.js`
- `node --check qa-tests.js`
- `node qa-tests.js`
- `node check-load.js`
- `git diff --check`
- HTML ID 静态映射：351 个 ID 无重复；三个静态扫描“缺失”项确认由 RJ 导入进度组件运行时动态创建，并非缺失节点。
- 原生 `alert` / `confirm` / `prompt` 静态检查：业务代码继续使用站内对话框。
- Object URL 路径审查：移动端导出预览关闭/替换时撤销；收藏不可见封面撤销；JSON 和 PC 下载 URL 定时撤销。
- Canvas 路径审查：分页、九宫格、速评、三宫格及独立修图的临时 Canvas 均有释放或失败清理路径。
- 导出按钮审查：主导出和独立修图导出均在 `finally` 恢复按钮状态。
- 多页顺序审查：首页索引 0，续页按数组索引顺序导出，文件页码为 01…N。
- 移动端根滚动结构审查：`html/body` 有界且禁止根滚动，`.app` 为 `100dvh` 内部滚动容器，未被本轮改动。
- 变更边界确认：未修改 `index.html`、`css/style.css`、模板 Canvas 绘制值、主题变量、数据库版本/store 或公开 JSON 版本。

## 第二遍核心链路回归（静态/逻辑）

已按以下顺序复核调用链和状态边界：

创建内容 → 上传 BK → 应用图片编辑结果 → 新增续页 → 切页并提交当前续页 → 保存队列 → 恢复状态与图片引用 → 分模板导出 → JSON 导出 → 二次确认清空 → JSON 导入 → 保存完成后再次进入导出路径。

静态/逻辑层面未发现页索引错位、图片 key 与模板格索引串位、模板状态串写或多页顺序反转。

## 保留风险 / 人工验证清单

以下项目需要真实浏览器或设备观测，或修复会改变现有产品行为，因此本轮未强行修改：

1. PC 与手机五个模板的实际 PNG 视觉一致性：字体加载、换行、行距、emoji fallback、特殊符号、裁切、水印、透明度和超长文本视觉。
2. iOS Safari：键盘弹起/收起、视觉视口变化、内部滚动、textarea caret、body scroll lock、图片工具条、裁切/打码和导出预览。
3. 内存曲线：图片 data URL ↔ IndexedDB 引用缓存 Map 在会话内不会主动按当前引用裁剪；连续替换大量不同大图时可能增长，需用浏览器 Memory 工具量测后再决定缓存策略。
4. 图片孤立记录采用 24 小时宽限期，不会在替换/删除后立即清除；这是现有防误删策略。需跨时间或手动存储清理验证最终 GC。
5. PC 多页/连续导出 Object URL 保留 30 秒后撤销；高频大量导出可能形成短时内存峰值。
6. 自动保存基于异步 IndexedDB。输入后在 80 ms 内强制刷新/关闭时，`beforeunload/pagehide` 发起的异步保存不能由浏览器绝对保证完成；若要完全消除此风险需要改变持久化策略，超出本轮低风险修复范围。
7. 页面初始化等待字体和 IndexedDB 恢复期间，用户若立即输入，恢复结果仍可能覆盖这段输入；安全修复通常需要加载锁或状态合并，会改变 UI/产品流程。
8. 主 BK、格子 BK 和收藏详情 BK 的“移除”仍是现有单击删除行为；补二次确认会改变交互，本轮按“不改变产品逻辑”保留。
9. `localStorage` API 整体不可用（隐私策略直接抛出 SecurityError）时仍有若干同步偏好读写点可能影响启动；这不同于“值为非法 JSON”，完整兼容需要封装所有偏好访问，属于较大范围改造。

## 数据安全确认

- 未升级、删除或重建任何 IndexedDB 数据库、store 或版本。
- 未修改图片 key 格式、状态 schema 版本、公开项目 JSON v1 或收藏备份 v1/v2/v3 兼容规则。
- 未执行数据清理、迁移命令或真实用户数据写入。
- 正常的现有 IndexedDB、`localStorage` 和 JSON 数据读取行为保持不变。
- 新增防御只影响异常类型：坏值被忽略或降级为空偏好；不会主动删除原始坏记录。
- 旧裸对象 JSON 仍可导入，公开 wrapper 仍可导入和导出，多余字段继续被忽略。

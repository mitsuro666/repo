# 项目视觉设计规范

本规范适用于乙女游戏记录卡生成器的页面界面、功能模块、卡片模板和导出画布。
它以项目现有设计为基础，用于保证后续新增或修改内容保持一致。

## 1. 整体风格

本项目采用“少女系收藏手帐 + 柔和桌面工具”的视觉方向。

- 基调：温柔、轻盈、精致，带有手帐和收藏感。
- 主色倾向：粉色、玫瑰色、奶油白、浅薄荷绿。
- 背景倾向：低对比度的柔和渐变和径向光晕。
- 容器倾向：半透明白色、圆角、轻微毛玻璃和柔和阴影。
- 装饰倾向：细线、虚线、星星、爱心、丝带和轻量花纹。
- 避免：高饱和大面积纯色、厚重阴影、尖锐工业风和干扰内容的复杂装饰。

## 2. 字体

- 全站默认使用 `BlackSugarPlumCandy`。
- 中文字体使用现有系统回退字体，优先保证中文显示稳定。
- 标题使用较高字重和较大的字号，体现收藏卡片和标题装饰感。
- 正文、表单和辅助信息保持足够字重，避免浅色细字在渐变背景上难以阅读。
- 标题可以使用描边和柔和文字阴影，但正文不使用明显描边。
- 输入框、按钮和文本域继承全站字体，避免出现系统字体跳变。

## 3. 全站色彩

### 3.1 基础界面色

- 主文字：深紫棕色，用于主要文字和普通操作。
- 次要文字：灰紫色或低透明度文字，用于说明、统计和辅助信息。
- 主强调色：玫瑰粉，用于主要按钮、选中状态和重要链接。
- 深强调色：深玫瑰色，用于标题、激活状态和强调文字。
- 辅助色：浅薄荷绿，用于平衡粉色、装饰线和卡片中的辅助信息。
- 背景色：奶油白、浅粉、浅绿和浅橙的低对比度渐变。
- 点缀色：金色只用于少量收藏感、等级感或装饰性元素。

### 3.2 使用原则

- 粉色是主视觉，不应让所有元素都使用同等强度的粉色。
- 深玫瑰色用于文字和重点，浅玫瑰色用于背景、边框和装饰。
- 薄荷绿作为平衡色，不承担主要操作语义。
- 主题卡片可以改变强调色，但必须保持文字、背景和边框的可读性。
- 全站通用界面优先使用现有的 `--ink`、`--muted`、`--rose`、`--rose-deep`、`--cream`、`--mint`、`--gold` 和 `--shadow`。
- 卡片模板使用独立的 `--card-*` 语义变量，不能直接借用全站界面颜色代替主题色。
- 新增颜色前先判断它是否已有对应的语义变量，避免在不同模块中散落相近但不一致的固定色值。

## 4. 页面背景与容器

- 页面使用浅色渐变背景，并配合局部径向光晕增加柔和层次。
- 主内容区域保持居中，沿用现有统一最大宽度和页面边距。
- 页面标题位于内容顶部中央，左右可使用镜像装饰线或轻量装饰图形。
- 页面级容器使用浅色半透明背景、细边框和较大的圆角。
- 工具页、收藏页和图片编辑页可以使用更克制的容器装饰，避免影响功能操作。
- 容器阴影应柔和、低透明度，主要用于区分层级，不制造厚重悬浮感。
- 同层级容器应使用一致的圆角、边框透明度和阴影强度。

## 5. 页面标题与品牌区域

- 主标题使用粉色高亮、较大字号和白色描边。
- 标题允许使用柔和的粉色投影，增强装饰感。
- 副标题使用胶囊形浅色容器，搭配细边框和内部高光。
- 标题装饰线应左右对称，不能抢过文字本身。
- 不同页面的标题区域应保持相同的视觉语言和垂直节奏。
- 新页面不应随意引入与现有站点标题不一致的独立品牌头部。

## 6. 导航

- 桌面端主导航固定在页面左侧，采用浅色半透明面板。
- 导航面板使用较大的圆角和轻微毛玻璃效果。
- 普通导航项保持透明背景，激活项使用白色背景、粉色边框和轻微阴影。
- 导航文字应比普通辅助文字更醒目，但不超过页面主标题的视觉权重。
- 移动端导航应转换为适合窄屏的布局，不依赖固定左侧空间。
- 导航图标和文字保持统一间距与垂直对齐。
- 新增导航项应复用现有状态样式，不建立新的导航视觉体系。

## 7. 按钮与操作控件

- 主要按钮采用玫瑰粉或粉色渐变，文字使用白色。
- 次要按钮使用白色或半透明白色背景、粉色边框和深玫瑰色文字。
- 低优先级操作使用透明背景，依靠文字和轻边框表达。
- 常规按钮使用中等圆角；工具栏、标签和紧凑型操作可以使用胶囊形圆角。
- 主要按钮可使用柔和粉色阴影，但阴影不能过重。
- 图标按钮应保持明确的点击区域，并提供可识别的悬停、聚焦和激活状态。
- 删除、移除等危险操作使用更深的红玫瑰色，但不使用刺眼纯红作为全局主色。
- 同一操作区域只突出一个主要操作，避免多个高权重粉色按钮互相竞争。

## 8. 表单与编辑状态

- 输入框采用无填充或半透明浅色背景，配合细边框和圆角。
- 标签使用深色或强调色，保证与背景有足够对比度。
- 输入框获得焦点时，使用粉色外轮廓、浅色背景或轻微阴影提示当前编辑位置。
- 文本域、内容编辑区和卡片输入区域尽量保持无干扰背景。
- placeholder 使用低对比度辅助色，不能与实际内容混淆。
- 编辑区域尽量与导出卡片的视觉样式一致，但编辑控件本身优先保证可操作性。
- 文件上传区域使用虚线边框、柔和底色和居中提示，体现“可放置内容”的状态。
- 同组字段应沿统一轴线排列，并由父级统一控制间距，避免逐项偏移。

## 9. 卡片模板视觉

卡片模板与普通网站模块使用不同的视觉变量和规则。卡片模板视觉规则也适用于对应的导出画布。

### 9.1 共通特征

- 卡片整体使用奶油白、浅粉、浅绿或主题派生背景。
- 卡片内容使用深色文字，辅助信息使用主题弱化色。
- 图片区域通常采用 4:3 比例，并使用圆角、虚线占位边框或白色描边。
- 卡片内部使用细线、虚线、局部色块和低透明度装饰。
- 主题强调色用于标题、标签、评分、边框和局部装饰。
- 薄荷色用于平衡主强调色，并作为第二装饰色。
- 装饰元素应围绕内容布局对称或建立明确对齐轴，不能随意漂浮。
- 纯白只用于对比、高光或图片描边等主题无关场景。

### 9.2 主题变量

- 模板颜色必须从 `--card-ink`、`--card-muted`、`--card-accent`、`--card-accent-deep`、`--card-line`、`--card-mint`、`--card-cover-bg` 和 `--card-bg-*` 等语义变量派生。
- 需要浅色、深色或透明变体时，优先使用 `color-mix()` 和现有变量。
- 新增主题相关颜色时，先在每个主题块中补齐新的 `--card-*` 变量，再由模板使用。
- Canvas 导出绘制必须接收由当前主题派生的颜色，不在绘制逻辑中硬编码某一套主题色。

### 9.3 模板布局

- `grid9` 强调九宫格、收藏卡片和标题装饰。
- `quick` 使用 2×6 的信息卡排列，突出封面、标题和短评。
- `trio` 使用三行横向信息块，突出封面与右侧资料区域。
- 重复信息块使用统一间距，不通过逐项偏移修正布局。
- 卡片内标题、标签、评价和编号应保持稳定的左对齐、右对齐或中心轴。
- 新模板应先确定清晰的分区、对齐轴和统一间距，再添加装饰。

### 9.4 预览与导出一致性

- 新模板和新标题样式必须同步实现页面预览和导出画布。
- 页面预览是导出视觉一致性的基准。
- 预览与导出应匹配结构、尺寸、间距、颜色、边框、阴影、装饰、字号、换行和位置。
- 预览调整阶段默认同步调整导出。
- 导出确认阶段只修正导出差异，除非明确决定同时调整预览。
- 样式专属修正应限制在对应版本和样式内，避免影响其他模板。

## 10. 图片编辑器与工具区域

- 图片编辑器属于功能型模块，视觉可以比卡片模板更克制。
- 工具栏使用白色或半透明白色容器，配合细边框和轻微阴影。
- 工具图标使用统一尺寸和线性视觉风格。
- 当前选中工具使用粉色强调色或浅粉背景。
- 画布区域应保持足够对比度，让图片内容优先于装饰。
- 空画布使用虚线边框、居中提示和简短说明。
- 工具按钮不能因为装饰性圆角、阴影或背景效果而降低可识别性。
- 桌面端可在悬停时显示图片工具；触屏端必须提供点击后可见或始终可见的等价操作。

## 11. 收藏与数据页面

- 收藏列表以内容图片为主要视觉焦点。
- 卡片封面使用统一比例，标题和元数据位于图片下方。
- 鼠标悬停时可以出现轻微上移、阴影和标题覆盖层。
- 覆盖层必须保证文字可读，且不能遮挡图片主体过多。
- 列表视图和网格视图共享相同的文字、标签和状态样式。
- 标签使用轻量边框或浅色底，不使用过重的彩色块。
- 收藏详情页使用更宽松的布局、较大的标题和分组信息区域。
- 详情页编辑状态通过浅色背景、焦点轮廓或细边框清楚表现。
- 数据密集区域可降低装饰密度，但仍沿用全站文字色、强调色和间距体系。

## 12. 弹窗与浮层

- 弹窗背景使用半透明深色遮罩，并配合轻微毛玻璃效果。
- 弹窗主体使用奶油白或白色、较大圆角和柔和阴影。
- 弹窗标题使用深玫瑰色或项目强调色。
- 弹窗内容区域保持足够内边距，按钮区域与正文之间有明确间距。
- 重要确认弹窗应让主要操作和取消操作具有清晰的视觉层级。
- 浮层不使用过度复杂的边框、装饰或多层阴影。
- 同类弹窗应共享宽度策略、圆角、遮罩和操作区样式。

## 13. 响应式表现

- 桌面端保留左侧导航、居中内容区和较宽的卡片预览。
- 小屏幕上导航、工具栏和外部操作按钮允许换行或转换为紧凑布局。
- 图片编辑工具在移动端使用适合触控的按钮尺寸。
- 卡片预览可以缩放，但不能破坏卡片内部比例和对齐关系。
- 移动端不能依赖 hover 显示关键工具；必要操作应通过点击或始终可见的控件提供。
- 窄屏环境下优先保留标题、主要编辑区、保存和导出操作。
- 响应式调整应优先改变父级布局，不为每个子元素增加零散偏移。

## 14. 可访问性与视觉可读性

- 正文、图标、边框、焦点和状态提示应与背景保持足够对比度。
- 选中、错误、禁用和成功状态不能只依赖颜色表达。
- 键盘焦点必须清晰可见，且不能被容器裁切。
- 图标按钮应有足够点击范围和可访问名称。
- 装饰元素不承载用户必须理解的唯一信息。
- 较大字号、页面缩放和长文本不应破坏主要操作与内容结构。

## 15. 项目视觉检查清单

- [ ] 是否保持粉色、奶油色和薄荷色的整体柔和基调？
- [ ] 全站界面与卡片模板是否分别使用正确的语义变量？
- [ ] 是否避免新增散落的主题相关固定颜色？
- [ ] 页面标题、导航、容器和按钮是否保持统一的圆角与间距语言？
- [ ] 主要操作是否明显，同时避免多个按钮争夺视觉焦点？
- [ ] 卡片模板是否保持主题可切换？
- [ ] 卡片预览与导出是否使用相同的结构、颜色和几何关系？
- [ ] 图片、输入框、标签和装饰线是否有稳定比例与对齐关系？
- [ ] 重复元素是否由父级统一控制布局和间距？
- [ ] 装饰是否服务于少女系收藏感，而没有干扰内容编辑？
- [ ] 悬停、聚焦、激活、禁用和加载状态是否清楚？
- [ ] 移动端是否不依赖 hover，且控件适合触控？
- [ ] 文字、图标、焦点和状态是否具备足够对比度？


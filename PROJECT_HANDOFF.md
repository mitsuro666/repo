# 项目交接说明：乙抓记录模板

这份文件给新开的 Codex 窗口快速接手用。先读 `AGENTS.md`，再读这份文件；`AGENTS.md` 是硬规则，这份是项目背景、模块结构和开发习惯。

## 项目是什么

这是一个单页乙女向 drama CD / 音声作品记录卡生成工具。目标是让用户填写或导入作品信息，上传或导入 BK，做简单 SNS 安全处理，然后导出适合发布的记录图。

项目没有构建步骤、没有包管理器。直接打开 `index.html` 可以运行，也部署在 GitHub Pages。

核心定位：

- 乙抓 / 乙女向音声作品记录模板，不是通用设计软件。
- 所有数据尽量在浏览器本地处理和保存。
- 体验优先级：能快速填、能好看导出、手机端能用。
- 用户会自己做视觉检查；Codex 不要跑复杂浏览器验证。

## 主要文件

- `index.html`：页面结构和固定 UI 文案。尽量只改结构，不把大量逻辑写回 HTML。
- `css/style.css`：全部样式，包括 PC / 手机响应式、模板色卡、图片编辑器、app shell。
- `js/app.js`：全部业务逻辑，包括状态保存、模板切换、色卡、导入导出、图片编辑器、DLsite 导入。
- `font/BlackSugarPlumCandy-Bold.ttf`：模板和界面使用的字体。
- `stamp/`：内置贴纸源 PNG。
- `stamp-data/`：贴纸 base64 备份，用于 `file://` 环境和避免 canvas 污染。
- `tool-icons/`：图片编辑器工具图标。
- `dlsite-worker.js`：Cloudflare Worker，用于代理 DLsite 商品 JSON，解决 CORS。
- `ENCODING_NOTES.md`：编码问题备忘，改 JS 中文字符串前一定要看。
- `keyword-display-checklist.txt`：DLsite 关键词展示/不展示清单。

## 当前页面结构

应用目前是三页式 app shell：

1. `模板编辑`
   - 当前主功能。
   - 包含模板样式、模板色卡、清空内容、导入数据、导出数据、导出图片。
   - PC 端是左侧栏 + 顶部工具栏；手机端是底部悬浮主菜单 + 顶部移动端操作区。

2. `修图工具`
   - 从模板图片编辑器独立出来的单独修图模式。
   - 主要服务乙抓 BK，不追求任意比例图片的完整 Photoshop 体验。
   - 支持上传图片、马赛克、模糊、橡皮擦、撤销、重做、贴纸、清空、移除图片、导出图片。

3. `我的收藏`
   - 目前是占位/初版框架。
   - 未来计划保存多个 repo 条目，点进条目可重新编辑。

## 模板样式

目前已存在多种模板入口，具体以 `index.html` 的 `.template-button` 和 `js/app.js` 的 `setTemplate()` / `templateSize()` 为准。

常见模板：

- `full`：完整版，1080 x 1440，主力模板。
- `compact`：简略版，600 x 800，BK + 标题 + 基本信息 + 播放组件。
- `grid9`：九宫格方向的新模板。
- `quick`：速评版方向的新模板。
- `trio`：三宫格方向的新模板。

注意：

- 新增模板时，要同时处理预览、导出尺寸、文件名、JSON 保存恢复、手机端缩放。
- 用户非常在意预览和导出的一致性。改模板布局时要同步导出逻辑。

## 模板色卡

色卡只影响“模板卡片内部”，不要把整个 app shell 和工具栏一起换色。工具栏整体仍是项目固定玫粉 UI。

当前色卡大致包括：

- 抹茶莓酪
- 樱花星冰
- 脏脏莓咖
- 午后宝石
- 月光碎片
- 薄荷气泡
- 玻璃微光
- 溶于茜色
- 群青沉眠
- 杏桃舒芙

色卡相关原则：

- 选中色卡后，完整版和简略版都要同步换色。
- 导出图片必须使用当前色卡。
- JSON 导入/导出必须保存并恢复当前色卡。
- 同一主题内，大模块框、评价框等主框背景应该统一，避免同主题内“这里白那里粉”的混乱。
- 星星、字幕按钮、价格标签、播放组件等特殊部件经常需要单独检查主题色。

## 图片编辑器

图片编辑器是最容易出问题的部分，改动要特别小心。

目前支持：

- 马赛克
- 模糊
- 橡皮擦
- 撤销 / 重做
- 画笔大小
- 贴纸
- 上传自定义贴纸
- 清空
- 完成 / 导出图片
- 移除图片（独立修图工具和 BK 区域）

重要原则：

- 模板内编辑图片：点 BK 上的 `编辑` 进入弹窗，点 `完成` 应把结果反映回模板。
- 弹窗右上角 `X` 等同取消，取消不应保存本次编辑。
- 已完成过的编辑，下次再打开编辑器应加载上次状态，能继续撤销/重做。
- 独立修图工具没有模板弹窗的取消语义，主要是上传、编辑、导出、移除。
- 手机端不要让“返回全局”按钮被底部菜单挡住；关掉图片弹窗后也要恢复。

## Canvas / 图片导出安全

这是项目踩坑最多的地方。

浏览器如果把跨域图片或 `file://` 图片直接画到 canvas 上，调用 `toDataURL()` 会报：

`SecurityError: Tainted canvases may not be exported`

处理原则：

- 导出前，所有会画进 canvas 的图片都必须先本地化为安全的 data URL。
- 贴纸不能直接从 `stamp/*.png` 画入导出 canvas；要优先用 `stamp-data/` 中的 base64 备份。
- 修改 `stamp/` 后，必须运行 `python generate_stamp_data.py` 重新生成 `stamp-data/`，然后硬刷新。
- 用户上传图片、RJ 导入 BK、贴纸图片，最终都应走同一套安全图片加载流程。
- 不要为了预览方便，把不安全图片直接用于导出。

相关函数主要在 `js/app.js`：

- `localizeImageToPngDataUrl`
- `rasterizeImageToPngDataUrl`
- `ensureStickerImage`
- `ensureUsedStickerImages`
- `updateEditedCoverFromEditor`
- `exportStandaloneImageFromEditor`

如果再遇到“贴纸能显示但保存失败”，优先检查贴纸是否真的转成 data URL 后再导出。

## DLsite / RJ 导入

输入 RJ 号可以导入作品信息，走 Cloudflare Worker：

`https://dlsite-rj-import.shuiyingsheng.workers.dev/`

导入范围目前包括：

- BK
- 标题
- CV
- 社团
- 现价
- 关键词
- 中文版本状态

导入规则：

- 如果目标字段已有内容，要弹出确认：已填写部分信息，是否覆盖？
- 如果所有目标字段都空，可以直接导入。
- BK 自动导入后，应跟手动上传一样可以编辑、模糊、马赛克、贴纸、导出。
- BK 建议用户使用 `编辑` 功能手动处理到适合 SNS 展示。
- 有中文版本时默认选中 `字幕`；没有中文版本时选中 `无中文`。
- 关键词只展示 SFW 关键词，未知关键词不显示，最多显示前 8 个。

关键词映射逻辑在 `js/app.js`，确认展示清单时参考 `keyword-display-checklist.txt`。

## 状态保存

本地保存使用 `localStorage`，key 在 `AGENTS.md` 里有写：

`otome-record-card-v1`

需要保存/恢复的内容包括：

- 当前页面
- 当前模板
- 当前色卡
- 表单文本
- 价格 / 折扣
- 评分
- 中文状态
- 关键词
- BK 原图、编辑结果、mask、贴纸状态、撤销重做状态
- 独立修图工具状态

任何新增功能，如果用户刷新后应该保留，就要接入保存/恢复和 JSON 导入导出。

## 导入 / 导出 JSON

用户可以通过 `导出数据` 备份当前编辑状态，再用 `导入数据` 恢复。

原则：

- 完整版、简略版、当前模板样式、当前色卡、图片编辑状态都要保存。
- JSON 是用户跨设备迁移的主要方式，字段名不要随便改。
- 如果必须改字段，尽量保留旧字段兼容。

## 文件命名

模板导出图片命名规则倾向于：

`RJ号_repo_模板英文_色卡英文.png`

独立修图工具导出图片倾向于按日期命名，不要叫 `record-card`。

## 手机端原则

手机端是重点，但不要强行塞进 PC 端布局。

目前倾向：

- 底部悬浮主菜单：`模板编辑` / `修图工具` / `我的收藏`。
- 顶部只保留当前页关键操作。
- 模板编辑中，色卡和模板样式在一个常驻白色区域内切换。
- 手机端默认显示整张模板；点击部分区域可聚焦放大，回全图按钮不能被底部栏挡住。
- 图片编辑器手机端要优先可用，不追求复杂缩放画布。

## PC 端原则

PC 端目前向“普通软件 App Shell”靠拢：

- 左侧固定侧边栏。
- 顶部工具栏。
- 二级菜单区。
- 中央内容区。

用户不喜欢“多个圆角框一层套一层”。做 UI 时尽量：

- 一级区域清楚。
- 二级按钮统一尺寸。
- 不要多余标题和外框。
- 图标使用 inline SVG，不要再用 CSS 硬画菜单图标。

## 编码注意

这个项目之前多次出现中文变问号、导出问号、图标问号。

必须遵守：

- 改 JS 里的中文 UI 字符串前，先读 `ENCODING_NOTES.md`。
- JS 里新增中文优先用 `String.fromCharCode(0x....)` 或已有项目方式。
- HTML 固定文字可用 HTML entities。
- 不要用可能导致 PowerShell 编码破坏的写文件方式。
- 手动编辑尽量用 `apply_patch`。

## 运行和检查

用户明确要求：不要跑复杂检查，不要浏览器自动化，不要截图验证。

改完代码只做基础检查：

```powershell
node -e "const fs=require('fs'); const s=fs.readFileSync('js/app.js','utf8'); new Function(s); console.log('script ok');"
```

如果改了 HTML 内联脚本结构，可按实际情况检查。

不要启动复杂测试，不要用 Playwright 视觉验证，实际效果交给用户看。

## 开发沟通习惯

用户喜欢快速迭代，会自己截图指出细节。回复时：

- 先改，不要长篇解释。
- 中途更新简短说明正在改什么。
- 最后说明改了哪些点、是否做了基础检查。
- 不要说“我视觉检查过了”，因为用户要求视觉检查交给她。
- 如果有风险，比如 CORS、canvas 污染、localStorage 容量，要直接说明。

## 常见坑速查

1. 导出失败，报 `Tainted canvases may not be exported`
   - 检查 BK / 贴纸 / 远程图是否已本地化为 data URL。
   - 检查 `stamp-data/` 是否更新。

2. 贴纸显示但无法导出
   - 预览可能用了原图，导出必须用安全 data URL。

3. 手机端按钮遮挡
   - 检查底部悬浮菜单、返回全局按钮、图片编辑弹窗底部操作区。

4. 预览和导出不一致
   - 通常是 CSS 预览改了，但导出 canvas 或导出用 DOM 尺寸没同步。

5. 刷新后丢数据
   - 检查字段是否接入 `saveState` / 恢复逻辑 / JSON 导入导出。

6. 中文变问号
   - 先看 `ENCODING_NOTES.md`，不要直接用不安全编码写入 JS。

7. `file://` 可用性坏了
   - 检查是否引入了绝对 `/path`、ES module、或必须网络 fetch 的资源。


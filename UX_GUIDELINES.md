# 通用 UX 交互准则

这份文档适用于本项目及后续网站、Web 应用和管理界面的设计与开发。
它关注用户如何理解、操作和恢复，不规定具体品牌风格、颜色、字体或业务领域。

## 1. 核心原则

- **让系统状态可见**：用户应能知道当前发生了什么，例如加载中、保存中、已保存、失败或无结果。
- **使用用户熟悉的语言**：按钮、提示和错误信息应描述用户要完成的事情，避免内部术语、技术错误码和模糊表达。
- **保持一致**：相同的操作、状态和组件在不同页面中使用相同的名称、位置和行为。
- **优先防止错误**：通过合适的控件、合理的默认值、格式提示和即时约束，减少用户犯错的机会。
- **允许恢复**：用户应能撤销、取消、返回或重新尝试；无法恢复的操作必须提前说明影响。
- **减少记忆负担**：让用户能看到必要的信息、上下文和操作结果，不要求用户记住上一页内容或复杂规则。
- **保持聚焦**：界面只展示当前任务需要的信息，次要内容不应干扰主要操作。

## 2. 页面与导航

- 页面标题应清晰说明当前位置和主要任务。
- 导航结构、返回行为和面包屑应符合用户预期，不要让返回键产生意外结果。
- 进入新页面、打开弹窗或切换上下文后，应保留足够的上下文，让用户知道自己从哪里来、正在处理什么。
- 不要仅依靠颜色、位置或图标表达重要含义；必要时配合文字或可访问名称。
- 页面刷新、网络波动或暂时失败不应轻易丢失用户已经输入的内容。

## 3. 编辑、保存与离开

- 所有可编辑内容都应明确采用以下一种模式：自动保存、即时生效，或用户主动提交保存。
- 使用主动保存模式时，页面应区分“当前正在编辑的内容”和“已经保存的内容”。
- 用户修改内容后直接关闭页面、返回、切换上下文或离开当前流程时，应提示存在未保存修改。
- 未保存提示至少应提供：继续编辑、放弃修改；如产品支持，也应提供保存并离开。
- 用户没有实际修改内容时，不应弹出未保存提示。
- 保存期间应防止重复提交，并显示保存进度或处理中状态。
- 保存成功后应提供明确反馈，并更新保存状态；保存失败时应保留输入内容并提供重试方式。
- 对可能耗时或容易中断的编辑流程，应考虑草稿、自动保存或恢复上次编辑内容。
- 自动保存不应让用户误以为所有操作都可以撤销；必要时仍需提供撤销或历史记录。

## 4. 表单与输入

- 每个输入控件都应有清晰、持久可见的标签；不要只使用 placeholder 代替标签。
- 必填项、选填项、输入格式、长度限制和示例应在用户输入前说明。
- 使用最适合数据类型的控件，例如日期选择、数字输入、单选、多选或搜索选择。
- 默认值应谨慎使用，不能让用户误把默认值当成自己的选择。
- 表单布局应按自然填写顺序组织，相关字段分组，避免不必要的多栏布局。
- 允许粘贴、自动填充和密码管理器等常见输入方式，不要设置没有充分理由的限制。
- 错误提示应靠近对应字段，并清楚说明：哪里有问题、为什么有问题、如何修正。
- 提交失败后应保留用户已填写的内容，并将焦点或注意力引导到第一个需要修正的位置。
- 不要仅通过禁用提交按钮隐藏错误；如果禁用，仍应说明还缺少什么。
- 输入过程中不应过早显示令人困惑的错误；可根据场景在输入完成、失焦或提交时校验。

## 5. 操作、确认与破坏性行为

- 主要操作应有明确且唯一的视觉和语义优先级。
- 按钮文字应描述结果，如“保存”“删除账户”“提交申请”，避免只写“确定”“继续”。
- 高风险或不可逆操作应在执行前明确说明对象、影响和后果。
- 只有在操作确实不可逆、代价高或容易误触时才使用二次确认；低风险操作不应被过度打断。
- 确认弹窗中的主要操作应与用户意图一致，危险操作不能因位置或样式让人误触。
- 尽可能提供撤销、回收站、恢复或延迟执行机制。
- 操作完成后，应告诉用户结果以及下一步可做什么。

## 6. 状态、反馈与通知

- 每个页面和组件都应考虑：加载中、成功、失败、空状态、无权限和离线等状态。
- 加载反馈应与等待时间匹配；短操作避免闪烁，长操作应说明正在处理什么。
- 成功反馈应简短、明确，不阻断用户继续工作。
- 错误信息应面向用户解决问题，不要只展示“发生未知错误”。
- 网络或服务失败时，应区分“没有提交成功”和“状态未知”，避免用户重复提交或误以为数据已保存。
- Toast、Snackbar 等临时通知不应承载唯一的重要信息；关键错误和确认结果应在页面中保留。
- 空状态应解释为什么为空，并在可能时提供下一步操作，例如创建、搜索、导入或清除筛选。
- 长时间任务应提供进度、取消方式或明确的后台处理说明。

## 7. 弹窗、抽屉与临时界面

- 弹窗只用于需要用户暂时聚焦的任务，不要用弹窗承载完整而复杂的主流程。
- 弹窗标题应说明任务；关闭按钮应明显，并支持 Escape 关闭（除非存在必须先处理的内容）。
- 打开弹窗前的焦点应在关闭后恢复。
- 弹窗内存在未保存内容时，关闭行为应遵循“编辑、保存与离开”规则。
- 不要连续叠加多个弹窗；需要进一步说明时优先使用页面内内容或新的明确步骤。

## 8. 可访问性

- 所有功能都应支持键盘操作，且焦点顺序符合视觉和任务顺序。
- 焦点状态必须清晰可见，不得仅通过颜色微小变化表示。
- 表单控件、按钮、图标按钮和图片应有合适的可访问名称或替代文本。
- 颜色对比度、文字缩放、窄屏和高对比度环境下仍应保持可读和可操作。
- 错误、成功、加载和动态更新应以适合辅助技术的方式传达。
- 不要把时间限制、拖拽、悬停或声音作为完成任务的唯一方式。
- 点击区域应足够大，相关控件之间应有清晰间距，避免误触。

## 9. 响应式与设备适配

- 先保证核心任务在小屏幕和触控环境下可完成，再扩展桌面端能力。
- 内容、操作和表单不应因为屏幕变窄而被截断或隐藏到用户无法发现的位置。
- 移动端应避免依赖 hover；桌面端也不应把关键操作只放在 hover 状态中。
- 在不同输入方式下提供合适的反馈：鼠标、键盘、触控和辅助技术都应有明确状态。
- 横竖屏、缩放、长文本、较大字号和系统字体变化不应破坏主要布局。

## 10. 内容与微文案

- 文案应简洁、具体、可行动，优先告诉用户现在发生了什么以及可以做什么。
- 使用一致的术语；同一个概念不要在不同页面使用多个名称。
- 避免责备用户的措辞，例如把“你输入错误了”改为“请输入有效的邮箱地址”。
- 危险、隐私、权限、收费和数据处理相关信息应在用户做决定前说明。
- 日期、时间、数字、单位和语言应符合用户所在地区及产品约定。
- 文字长度变化不应导致按钮意义消失或主要操作无法使用。

## 11. 交付前检查清单

- [ ] 是否明确了哪些内容会自动保存、即时生效或需要主动保存？
- [ ] 修改后离开页面、关闭弹窗或刷新时，是否正确处理未保存内容？
- [ ] 是否覆盖加载、成功、失败、空状态、无权限和离线状态？
- [ ] 错误信息是否说明问题和解决方式，并保留用户输入？
- [ ] 破坏性操作是否有适度的确认或撤销机制？
- [ ] 键盘、焦点、屏幕阅读器和窄屏场景是否可用？
- [ ] 关键操作是否有清晰的文字和结果反馈？
- [ ] 是否避免了只靠颜色、图标、hover 或弹窗来传达关键信息？
- [ ] 页面刷新、网络失败或重复点击是否不会造成意外的数据丢失或重复操作？

## 参考

- [Nielsen Norman Group：10 Usability Heuristics for User Interface Design](https://www.nngroup.com/articles/ten-usability-heuristics/)
- [GOV.UK Design System](https://design-system.service.gov.uk/)
- [Material Design](https://m3.material.io/)
- [Microsoft Fluent Design Guidelines](https://learn.microsoft.com/en-us/windows/apps/design/)
- [Web Content Accessibility Guidelines (WCAG) 2.2](https://www.w3.org/TR/WCAG22/)

